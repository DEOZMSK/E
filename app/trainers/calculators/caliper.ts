export const calculateCaliper=(folds:number[],weightKg:number)=>{const s=folds.reduce((a,b)=>a+b,0);const avg=s/folds.length;const fatPercent=Math.max(4,Math.min(45,8+avg*0.55));const fatMassKg=(weightKg*fatPercent)/100;return{sumFolds:+s.toFixed(1),averageSkinfold:+avg.toFixed(1),fatPercent:+fatPercent.toFixed(1),fatMassKg:+fatMassKg.toFixed(1),lbmKg:+(weightKg-fatMassKg).toFixed(1)}};
